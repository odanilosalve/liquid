# LIQUID API - Variáveis de Ambiente
# Copie este arquivo para .env e configure os valores apropriados
# NUNCA commite o arquivo .env com valores reais no repositório

# CREDENCIAIS AWS (Obrigatórias para deploy)
AWS_ACCESS_KEY_ID=your-aws-access-key-id
AWS_SECRET_ACCESS_KEY=your-aws-secret-access-key
AWS_DEFAULT_REGION=us-east-1

# VARIÁVEIS OBRIGATÓRIAS (PRODUÇÃO)

# Autenticação JWT
JWT_SECRET_KEY=your-secret-key-minimum-32-characters-long
JWT_ALGORITHM=HS256
JWT_EXPIRATION_HOURS=24

# Ambiente de execução: dev, prod, production
STAGE=dev

# Nome da tabela DynamoDB para armazenar taxas de câmbio
CURRENCY_TABLE=currency-rates-dev

# Nome da tabela DynamoDB para armazenar usuários
USERS_TABLE=users-dev

# Origem permitida para CORS (use '*' apenas em desenvolvimento)
ALLOWED_ORIGIN=*

# URL da API externa de taxas de câmbio
EXCHANGE_RATE_API_URL=https://api.exchangerate-api.com/v4/latest

# Timeout em segundos para requisições à API externa (padrão: 5)
EXTERNAL_API_TIMEOUT=5

# VARIÁVEIS OPCIONAIS

# Nome do serviço (usado em logs e health checks)
SERVICE_NAME=liquid-api

# Moedas válidas para conversão, separadas por vírgula
CURRENCIES=USD,BRL,EUR,GBP,JPY

# Valor máximo permitido para conversão (padrão: 1000000000)
MAX_CONVERSION_AMOUNT=1000000000

# Valor mínimo permitido para conversão (padrão: 0.01)
MIN_CONVERSION_AMOUNT=0.01

# Headers permitidos nas requisições CORS (padrão: Content-Type)
CORS_ALLOW_HEADERS=Content-Type

# Métodos HTTP permitidos nas requisições CORS (padrão: POST, GET, OPTIONS)
CORS_ALLOW_METHODS=POST,GET,OPTIONS

# Tamanho mínimo da chave JWT em caracteres (padrão: 32)
JWT_SECRET_MIN_LENGTH=32

# Tempo de vida do cache em horas (padrão: 1)
CACHE_TTL_HOURS=1

# VARIÁVEIS PARA SEED DE USUÁRIOS
# Estas variáveis são usadas apenas durante o seed inicial de usuários
# Não são necessárias para o funcionamento normal da API

SEED_USER_ADMIN_PASSWORD=
SEED_USER_USER1_PASSWORD=
SEED_USER_TEST_PASSWORD=
SEED_PASSWORD_LENGTH=16
SEED_PASSWORD_CHARS=
SEED_USERS_CONFIG_FILE=seed_users_config.json
